{
  "name": "openai-compatible-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "vercel": "^31.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}